services:
  backend1:
    image: ghcr.io/embrs/asp-ci-demo-release:latest
    restart: always
    expose:
      - '5000'
    networks:
      - appnet
  backend2:
    image: ghcr.io/embrs/asp-ci-demo-release:latest
    restart: always
    expose:
      - '5000'
    networks:
      - appnet
  frontend1:
    image: ghcr.io/embrs/nuxt-ci-test:latest
    expose:
      - '3000'
    restart: always
    networks:
      - appnet

  nginx:
    image: nginx:alpine          # 使用輕量 Alpine 版本
    ports:
      - "80:80"                  # 映射 HTTP 端口
      - "443:443"                # 若有 HTTPS 需映射 443
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf  # 掛載自定義配置
      - ./nginx/ssl:/etc/nginx/ssl:/etc/nginx/ssl;          # 掛載 SSL 憑證
    depends_on:
      - backend1
      - backend2
      - frontend1
    networks:
      - appnet
networks:
  appnet:
